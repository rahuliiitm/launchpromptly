services:
  postgres:
    image: postgres:16-alpine
    container_name: aiecon-db
    environment:
      POSTGRES_USER: aiecon
      POSTGRES_PASSWORD: aiecon
      POSTGRES_DB: aiecon
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
